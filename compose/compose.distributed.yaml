services:
  api:
    build:
      context: ../api
      dockerfile: Dockerfile
    deploy:
      replicas: 3

  nginx:
    image: nginx:alpine
    ports:
      - "8000:8000"
    volumes:
      - ../proxy/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
